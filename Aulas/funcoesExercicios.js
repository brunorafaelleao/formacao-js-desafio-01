/*
//funcao que escreve meu nome
function escreveMeuNome(nome) {
  return `O nome digitado foi: ${nome}`;
}

function verficaIdade(idade) {
  if (idade >= 18) {
    return "Maior de idade";
  } else {
    return "Menor de idade";
  }
}

prompt = require("prompt-sync")();
let nome = prompt("Digite seu nome: ");
console.log(escreveMeuNome(nome));
let idade = parseInt(prompt("Digite sua idade: "));
console.log(`${nome}, você tem ${idade} anos e é ${verficaIdade(idade)}`);
*/

/*
function calcularMedia(nota1, nota2) {
  const media = (nota1 + nota2) / 2;
}

console.log(calcularMedia(5, 5)); */


